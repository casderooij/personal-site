title: Artifacts

sections:
  artifacts:
    type: pages
    label: Artifacts
    create:
      - artifact
    templates:
      - artifact
    info: "{{ page.date.toDate('d-m-Y') }} | {{ page.tags }}"
